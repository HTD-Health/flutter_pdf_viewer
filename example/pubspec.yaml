name: pdf_viewer_example
description: Demonstrates how to use the pdf_platform_view plugin.
version: 1.0.0+2

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  pdf_platform_view:
    path: ../

flutter:
  uses-material-design: true
